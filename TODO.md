I think it'd be simpler to have a single set of load/save ski button in the top bar of the app, rather than buttons that have the same funtion in each tab. Also, the ski ski definition file should contain all the parameters set in the program, including in the machining tabs (materials, core blank dimensions, CNC machine setup, cut setting etc) and these should be set on loading a ski definition file.

The app is currently quit sluggish when switching tabs and loading a ski definition file. Can you do any refactoring to improve the performance of the app? Generating G Code currently takes quite a while (especially core profiling long skis), and the app freezes during g code generation. Can you make it to that when the 'generate g code buttons' are pressed, this happens in the background so the app is usable in the meantime. The progress bars currently don't work that well and don't accurately report the g code generation progress.